
  # Recreate Page Design (Copy)

  This is a code bundle for Recreate Page Design (Copy). The original project is available at https://www.figma.com/design/yGVdWYpmRhxWwHRYYjzIhw/Recreate-Page-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  